# How to use:
# 1) This is the only env template in the repository (single source of truth).
# 2) Copy this file to .env when you want to override runtime defaults.
# 3) Frontend runtime defaults are in frontend-app/src/app/api/env.ts.
# 4) Backend runtime defaults are in backend-app/.../Infrastructure/Configuration/RuntimeSettings.cs.
# 5) You can override backend values either by env vars OR appsettings.Development.json.

# Docker / Postgres
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=workoutdb
POSTGRES_USER=workout_user
POSTGRES_PASSWORD=workout_pass

# Backend (.NET) env overrides
# Development can use the defaults below.
# Non-development must set explicit CORS origins and a unique 64+ character JWT secret.
ASPNETCORE_ENVIRONMENT=Development
ConnectionStrings__Default=Host=localhost;Port=5432;Database=workoutdb;Username=workout_user;Password=workout_pass
Jwt__Issuer=ApiModule
Jwt__Audience=ApiModule.Client

# Development-only secret. Do not use this value outside ASPNETCORE_ENVIRONMENT=Development.
# Example strong secret:
# powershell -NoProfile -Command "[Convert]::ToBase64String((1..64 | ForEach-Object { Get-Random -Maximum 256 }))"
Jwt__Key=dev-only-change-me-to-at-least-32-characters
Jwt__ExpiresMinutes=120

# Semicolon-separated origins. Required outside Development.
# Examples:
# CORS_ALLOWED_ORIGINS=http://localhost:3000
# CORS_ALLOWED_ORIGINS=https://your-web.example.com
CORS_ALLOWED_ORIGINS=http://localhost:3000

FORM_ANALYSIS_TIMEOUT_SECONDS=300
FORM_ANALYSIS_MAX_VIDEO_MEGABYTES=250
FORM_ANALYSIS_MAX_ANALYSES_PER_USER=50
FORM_ANALYSIS_RETENTION_DAYS=30

# Frontend (Next.js)
NEXT_PUBLIC_API_MODE=real
NEXT_PUBLIC_API_URL=http://localhost:5064/api
NEXT_PUBLIC_API_URL_REAL=http://localhost:5064/api
NEXT_PUBLIC_API_URL_MOCK=http://localhost:5064/api

# Capacitor / Android development
# Emulator:
# CAPACITOR_SERVER_URL=http://10.0.2.2:3000
# NEXT_PUBLIC_API_URL=http://10.0.2.2:5064/api
#
# Physical phone on the same Wi-Fi:
# CAPACITOR_SERVER_URL=http://YOUR_PC_LAN_IP:3000
# NEXT_PUBLIC_API_URL=http://YOUR_PC_LAN_IP:5064/api
